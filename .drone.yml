---
kind: pipeline
name: default

steps:
  - name: fetch
    image: plugins/git
    commands:
      - git fetch --tags

  - name: unit_test
    image: golang:1.11
    commands:
      - go test -mod=vendor -v ./...
    when:
      event: [ pull_request, tag ]

  - name: Goreleaser Build
    image: golang:1.11
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
    commands:
      - curl -sL https://git.io/goreleaser | bash
    when:
      event: [ tag ]
      status: [ success ]

  - name: Docker Build
    image: plugins/docker
    environment:
      DOCKER_USERNAME:
        from_secret: docker_username
      DOCKER_PASSWORD:
        from_secret: docker_password
    settings:
      auto_tag: true
      repo: gmorse81/boondoggle
    when:
      event: [ tag ]
      status: [ success ]

  - name: Goreleaser Build DryRun
    image: golang:1.11
    environment:
      GITHUB_TOKEN:
        from_secret: github_token
    commands:
      - curl -sL https://git.io/goreleaser | bash -s -- build
    when:
      event: [ pull_request ]
      status: [ success ]

  - name: Docker Build DryRun
    image: plugins/docker
    settings:
      auto_tag: true
      repo: gmorse81/boondoggle
      dry_run: true
    when:
      event: [ pull_request ]
      status: [ success ]

  # - name: docker_build_latest
  #   image: plugins/docker
  #   environment:
  #     DOCKER_USERNAME:
  #       from_secret: docker_username
  #     DOCKER_PASSWORD:
  #       from_secret: docker_password
  #   settings:
  #     tags:
  #       - latest
  #     repo: gmorse81/boondoggle
  #   when:
  #     event: [ tag ]
  #     status: [ success ]
---
kind: signature
hmac: 85233c37cad952e62dd7724b09a29d200b89685e918d2b7cda2f5866a832996c

...
